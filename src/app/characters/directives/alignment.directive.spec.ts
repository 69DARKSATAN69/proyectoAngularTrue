import { AlignmentDirective } from './alignment.directive';

describe('AlignmentDirective', () => {
  it('should create an instance', () => {
    const directive = new AlignmentDirective();
    expect(directive).toBeTruthy();
  });
});
