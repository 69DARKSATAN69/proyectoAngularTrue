import { DisplayDirectiveDirective } from './display-directive.directive';

describe('DisplayDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
