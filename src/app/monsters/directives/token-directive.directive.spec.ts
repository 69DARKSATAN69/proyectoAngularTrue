import { TokenDirectiveDirective } from './token-directive.directive';

describe('TokenDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TokenDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
