import { MonsterBackgroundDirective } from './monster-background.directive';

describe('MonsterBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new MonsterBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
